on:
  push:
    branches: [ release/* ]
  workflow_dispatch:

jobs:
  pre-release:
    uses: abbgrade/PsBuildTasks/.github/workflows/pre-release-windows.yml@fd9646ab810c231ce4709538886a71eb2c0b35aa
    with:
      module-name: PsBuildTasks
    secrets:
      ps-gallery-key: ${{ secrets.PS_GALLERY_KEY }}
